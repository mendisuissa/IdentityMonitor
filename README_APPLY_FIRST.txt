Apply this file on top of the current execute remediation UI.
This fixes the remaining TypeScript select onChange errors in RemediationPage.tsx
and aligns the option values with the strict union types used by the API.
